#Library Management System

A small Java console project for managing a library's books, members, and current loans. It lets a librarian add books, register members, view records, issue available books, and return issued books. The application prevents duplicate IDs and prevents a book from being issued more than once at the same time.
